{
  // Cloudflare Workers — static assets only (no Worker code / bindings).
  // Deploy with:  pnpm build && pnpm dlx wrangler deploy
  "name": "sizukutamago-portfolio",
  "compatibility_date": "2025-05-21",
  "assets": {
    "directory": "./dist",
    "not_found_handling": "404-page"
  }
}
